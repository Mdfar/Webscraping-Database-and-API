# Webscraping-Database-and-API
A config-driven Python pipeline that scrapes articles from multiple websites and stores them in SQLite, ready for later LLM re-processing. Adding a new site is a JSON file, not a code change.
